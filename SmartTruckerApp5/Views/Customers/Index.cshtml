@model IEnumerable<SmartApp4.Models.Customers>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cust_Contact)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cust_Contact)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomersKey }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomersKey }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomersKey })
        </td>
    </tr>
}

</table>
